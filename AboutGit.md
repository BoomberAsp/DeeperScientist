# 小团队 Git 分支管理规范

> 适用规模：**4人及以下** 的开发团队  
> 核心目标：保持主干随时可发布，简化流程，保证代码评审。

---

## 1. 基本原则

- `main` 分支是唯一长期分支，始终处于可部署状态。
- 所有开发工作通过短期功能分支完成，合并后即删除。
- 任何合并到 `main` 的操作必须通过 **Pull Request + 至少一人审查**。
- 自动化测试通过后才允许合并。(或许有些特殊情况需要人工确认，但原则上应尽量自动化)
- 提交信息遵循约定式提交，便于追溯。

---

## 2. 分支模型

只维护一条永久分支：`main`。  
其他均为临时分支，用完即删。

| 分支类型 | 命名规则 | 用途 | 来源/合并目标 |
|----------|----------|------|---------------|
| 主分支 | `main` | 生产就绪代码，所有发布基于此 | - |
| 功能分支 | `feature/<描述>` | 新功能、优化、重构等 | 从 `main` 拉出 → 合并回 `main` |
| 修复分支 | `fix/<描述>` | Bug 修复（无论线上还是测试发现） | 从 `main` 拉出 → 合并回 `main` |
| 热修复分支 | `hotfix/<描述>` | 紧急线上问题 | 从 `main` 最新发布标签拉出 → 合并回 `main` |
| 实验分支(可选) | `exp/<描述>` | 技术调研、原型验证，不保证合并 | 从 `main` 拉出，不必须合并 |

> **为什么不需要 `develop` 分支？**  
> 小团队并行开发量有限，直接在 `main` 上通过短分支 + 频繁集成，冲突少、反馈快，管理成本最低。

---

## 3. 分支命名规范

- **功能分支**：`feature/<简短描述>`  
  示例：`feature/login-page`、`feature/api-rate-limit`

- **修复分支**：`fix/<简短描述>`  
  示例：`fix/order-total-rounding`

- **热修复分支**：`hotfix/<简短描述>`  
  示例：`hotfix/payment-crash`

- **实验分支**：`exp/<简短描述>`  
  示例：`exp/new-state-manager`

- 描述统一使用**小写字母 + 连字符**，保持简短有意义。  
- 禁止使用个人姓名或日期命名。

---

## 4. 日常工作流程

### 4.1 新功能开发

1. 确保本地 `main` 最新：
   ```bash
   git checkout main
   git pull origin main
   ```
2. 创建功能分支：
    ```bash
    git checkout -b feature/<描述> 
    ```

3. 在本分支上开发，小步提交。每天至少一次将 ```main``` 的变动合并入自己的分支：
    ```bash
    git merge main
    ``` 
    或使用 rebase 保持历史清晰（团队约定一致即可）。
4. 完成后，推送分支并创建 Pull Request（PR），目标分支为 ```main```。
5. PR 标题使用约定式提交格式，如 `feat: 增加短信登录，描述中关联任务编号`。
6. 等待 CI 通过（编译、测试、代码扫描），并由至少一名队友审查通过。
7. 审查通过后，推荐使用 **Squash and Merge** 压缩为一条整洁提交，或根据情况使用普通合并（`--no-ff`）。
8. 合并后删除远程和本地功能分支。

### 4.2 Bug 修复（非紧急） 
+  从 ```main``` 拉出 ```fix/<描述>``` 分支，流程同功能分支。
+  合并到 ```main``` 后，如需立即上线，可打标签发布。

### 4.3 紧急线上修复 (hotfix) 
1. 找到当前线上版本标签（如 v1.2.0），从该标签拉出分支：
    ```bash
    git checkout -b hotfix/<描述> v1.2.0 
    ```
2. 修复问题，提交。
3. 创建 PR 合并到 main。
4. 合并后，在 main 上立即打新版本标签：
    ```bash
    git checkout main

    git pull origin main

    git tag -a v1.2.1 -m "hotfix: 支付崩溃修复"

    git push origin main --tags 
    ```
5. 删除 hotfix 分支。

## 5. 版本发布与标签 
+  每次正式发布都在 `main` 分支上打语义化版本标签：`v<主版本>.<次版本>.<修订号>`例如：`v1.3.0`, `v1.3.1`
+  标签使用附注标签（`-a`），注明变更简要。
+  由负责人推送标签后，触发 CI/CD 部署到生产环境。
+  预发布（如测试环境）可打后缀标签：`v1.3.0-rc.1`。

## 6. 提交信息规范 
严格执行约定式提交，每条信息格式为：
```text
<type>(<scope>): <简短描述>


[详细描述]


[关联 issue] 
常用类型：


类型       用途
feat     新功能
fix      Bug 修复
docs     文档
style    代码格式（不影响逻辑）
refactor 重构
perf     性能优化
test     测试
chore    构建、依赖、脚本等
hotfix   紧急修复（与 fix 类似，但强调线上补丁）

示例：
text
feat(auth): add OAuth2 login via Google

Users can now log in using their Google account.

Closes #42 
```
**团队约束**：所有 PR 合并时，确保最终合并后的提交信息符合规范，可通过 Squash 时重写消息实现。

## 7. 代码审查 (Code Review) 规则 
+  最少一人审查通过才能合并（四人团队默认“除作者外至少一人”）。
+  审查重点：逻辑正确、安全漏洞、性能隐患、代码可读性、测试覆盖。
+  鼓励小 PR（尽量不超过 400 行变更），加快审查速度。
+  反馈需在 1个工作日内 给出，超时可主动 @ 队友提醒。
+  PR 不得包含调试代码、临时文件，合并前必须清除。

## 8. CI/CD 集成要求 
+  每个 PR 触发：代码编译、单元测试、静态代码检查。
+  合并到 main 后自动部署到测试/预发布环境。
+  推送版本标签（v*）后自动部署到生产环境（或手动确认后部署）。
+  只有 CI 全绿且审查通过，才允许合并 PR。

## 9. 协作约定 
+  保持 main 干净：合并前确认本地已处理所有与 main 的冲突，确保构建通过。
+  及时清理：合并后立刻删除远程和本地临时分支。
+  避免大型重构集中进行：拆分多个小 PR 递进合并。
+  沟通先行：涉及共用模块的改动，在动手前先与团队简短时间内对齐。
+  结对编程的分支策略：若两人结对开发一个功能，可在同一 feature 分支上协作，频繁 push/pull，仍通过一次 PR 合并。

## 10. 常见问题 
**Q：多个功能需要同时测试，但还没合并到 main？**\
A：利用 CI 的预发布环境，可以临时合并多个 PR 到一条 integration-test 分支部署测试，测试通过后仍按原 PR 逐个合并到 main。\
**Q：不小心把半成品推送到 main？**\
A：立刻通知团队，使用 git revert 回退合并提交，不要 reset 共享分支。事后复盘，必要时设置 main 分支保护。\
**Q：实验性分支还要审查吗？**\
A：exp/ 分支不要求审查，但一旦决定转为正式功能，需按功能分支流程重新提交 PR 和审查。\
## 附：Pull Request 操作说明 
### 创建 PR 
1. 推送分支在功能/修复分支开发完成后，先确保已合并 main 最新内容并解决冲突，然后推送到远程仓库：
    ```bash
    git push origin feature/<描述> 
   ```
2. 在平台上创建 PR
+  源分支：feature/<描述>（或其他临时分支）
+  目标分支：main
+  标题：遵循约定式提交格式，如 feat: 增加短信登录
* 描述栏填写：
  - 做了什么改动
  - 测试情况（如何手动测试、自动化测试结果）
  -  关联的 issue 或任务编号（如 Closes #12）
  -  是否有破坏性变更、注意事项
+ 如果还在开发中，请标记为 Draft（草稿），防止误合并。
3. 请求审查指定至少一名团队成员为 Reviewer（审查者），发送通知或在团队频道提醒。
### 审查 PR 
1. 打开 PR，逐行查看代码变更。关注点：
  * 逻辑是否正确，边界条件是否处理
  *  是否存在安全或性能隐患
  *  代码风格是否符合项目约定
  *  是否有充分的自动测试或清晰的测试说明
2. 在具体代码行可添加评论，提出疑问或改进建议。避免人身攻击，就事论事。
3. 审查结论：
*  通过（Approve）：代码合格，可准备合并。
*  评论（Comment）：有轻微问题或建议，但不阻断合并。
*  请求修改（Request Changes）：必须修改后才能合并，此时应明确指出问题所在。
4. 审查反馈应在1 个工作日内完成，超过时间可主动催促。
### 合并 PR 
1. 作者根据审查意见修改代码并推送新提交，审查者会收到通知并可重新审查。
2. 所有 Review 项标记为已解决、CI 自动检查全部通过后，由原作者或审查者执行合并。
3. 合并方式选择：
*  推荐 Squash and Merge：将分支上所有提交压缩为一个规范格式的提交，使 main 历史简洁。
*  如果功能分支的提交历史有保留价值，可使用普通 Merge Commit（--no-ff），保留完整线索。
4. 合并成功后，及时删除远程分支（平台会提示一键删除）和本地分支：
    ```bash
    git branch -d feature/<描述> 
    ```